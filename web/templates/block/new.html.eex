<h2>New block</h2>

<%= render "form.html", changeset: @changeset,
                        disabled: @disabled,
                        action: block_path(@conn, :create) %>

<%= link "Back", to: block_path(@conn, :index) %>
